# configmap as volume 
kind: Deployment
apiversion: aaps/v1
metadata:
  name: mywebdeploy
spec:
  replicas: 2
  selector:
    matchLabels:
      env: dev
template:
  metadata:
    name: mypo
    labels:
      env: dev
  spec:
    containers:
      - name: mycont
        image: suribau/web:v1.12
        env:
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: my-config
              key: MONGODB_URI
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: mongo-creds
              key: secret
        
               
